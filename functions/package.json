{
  "name": "lovequest-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for LoveQuest",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "keywords": ["firebase", "cloud functions", "lovequest"],
  "author": "LoveQuest",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
